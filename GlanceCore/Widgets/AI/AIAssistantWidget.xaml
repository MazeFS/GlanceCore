<widgets:BaseWidgetWindow x:Class="GlanceCore.Widgets.AI.AIAssistantWidget"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:widgets="clr-namespace:GlanceCore.Widgets"
        Title="AI Assistant" WindowStyle="None" Background="Transparent" AllowsTransparency="True"
        DataContext="{Binding RelativeSource={RelativeSource Self}}">

    <Grid x:Name="MainRoot" Width="300" Height="400" Margin="15">
        <Grid.Effect>
            <DropShadowEffect BlurRadius="25" ShadowDepth="8" Opacity="0.4"/>
        </Grid.Effect>
        <Border x:Name="BackgroundLayer" CornerRadius="{Binding WidgetCornerRadius}" BorderBrush="{Binding BorderColorBrush}" BorderThickness="1.5" ClipToBounds="True" Opacity="{Binding BgOpacity}">
            <Grid>
                <Border x:Name="GlassBase" CornerRadius="{Binding WidgetCornerRadius}">
                    <Border.Background>
                        <ImageBrush x:Name="WallpaperBrush" Stretch="Fill"/>
                    </Border.Background>
                </Border>
                <Border x:Name="MinimalBase" Background="{Binding BgColorBrush}" CornerRadius="{Binding WidgetCornerRadius}" Opacity="1"/>
                <Border Background="#60000000" CornerRadius="{Binding WidgetCornerRadius}"/>
                <Border x:Name="GlossBevel" CornerRadius="{Binding WidgetCornerRadius}" BorderBrush="#35FFFFFF" BorderThickness="1.5" Margin="1" IsHitTestVisible="False">
                    <Border.Background>
                        <RadialGradientBrush Center="0.5,0" RadiusX="0.9" RadiusY="0.6">
                            <GradientStop Color="#25FFFFFF" Offset="0"/>
                            <GradientStop Color="Transparent" Offset="1"/>
                        </RadialGradientBrush>
                    </Border.Background>
                </Border>
            </Grid>
        </Border>

        <Grid Margin="15">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

			<TextBlock Text="AI Assistant" Effect="{Binding TextGlowEffect}" FontWeight="Bold" Foreground="{Binding TextColorBrush}" FontSize="{Binding FontSize}" Margin="0,0,0,10"/>

            <ScrollViewer x:Name="ChatScroll" Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="0,0,0,10">
                <TextBlock Text="{Binding ChatHistory}" Foreground="{Binding TextColorBrush}" FontSize="{Binding FontSize}" TextWrapping="Wrap" LineHeight="20"/>
            </ScrollViewer>

            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="TxtInput" KeyDown="TxtInput_KeyDown" Background="#20FFFFFF" Foreground="White" BorderThickness="0" Padding="10,8" VerticalContentAlignment="Center">
                    <TextBox.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="8"/>
                        </Style>
                    </TextBox.Resources>
                </TextBox>
                <Button Grid.Column="1" Click="BtnSend_Click" Content="➔" Background="#40FFFFFF" Foreground="White" BorderThickness="0" Margin="8,0,0,0" Width="35" Cursor="Hand">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="8"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </Grid>
        </Grid>
    </Grid>
</widgets:BaseWidgetWindow>