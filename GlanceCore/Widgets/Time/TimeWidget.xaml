<widgets:BaseWidgetWindow x:Class="GlanceCore.Widgets.Time.TimeWidget"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:widgets="clr-namespace:GlanceCore.Widgets"
        Title="Time" WindowStyle="None" Background="Transparent" AllowsTransparency="True"
        DataContext="{Binding RelativeSource={RelativeSource Self}}">

    <Grid x:Name="MainRoot" Width="240" Height="120" Margin="15">
        <Grid.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="5" Opacity="0.4"/>
        </Grid.Effect>

        <!-- СЛОЙ 1: ФОН СТЕКЛА -->
		<Border x:Name="BackgroundLayer" CornerRadius="{Binding WidgetCornerRadius}" BorderBrush="{Binding BorderColorBrush}" BorderThickness="1.5" ClipToBounds="True" Opacity="{Binding BgOpacity}">
			<Grid>
				<Border x:Name="GlassBase" CornerRadius="{Binding WidgetCornerRadius}">
					<Border.Background>
						<ImageBrush x:Name="WallpaperBrush" Stretch="Fill"/>
					</Border.Background>
				</Border>
				<Border x:Name="MinimalBase" Background="{Binding BgColorBrush}" CornerRadius="{Binding WidgetCornerRadius}" Opacity="1"/>
				<Border Background="#20000000" CornerRadius="{Binding WidgetCornerRadius}"/>
				<Border x:Name="GlossBevel" CornerRadius="{Binding WidgetCornerRadius}" BorderBrush="#35FFFFFF" BorderThickness="1.5" Margin="1">
					<Border.Background>
						<RadialGradientBrush Center="0.5,0" RadiusX="0.9" RadiusY="0.6">
							<GradientStop Color="#25FFFFFF" Offset="0"/>
							<GradientStop Color="Transparent" Offset="1"/>
						</RadialGradientBrush>
					</Border.Background>
				</Border>
			</Grid>
		</Border>

		<!-- СЛОЙ 2: КОНТЕНТ (ВРЕМЯ) -->
		<Viewbox Margin="20" Stretch="Uniform">
			<StackPanel x:Name="TimePanel" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">

				<TextBlock Text="{Binding Hours}" Foreground="{Binding TextColorBrush}" FontWeight="Bold" VerticalAlignment="Center"/>

				<TextBlock x:Name="Separator1" Text="{Binding Separator}" Foreground="{Binding TextColorBrush}" Opacity="{Binding SeparatorOpacity}" Margin="4,-2,4,0" VerticalAlignment="Center"/>

				<TextBlock Text="{Binding Minutes}" Foreground="{Binding TextColorBrush}" FontWeight="Bold" VerticalAlignment="Center"/>

				<TextBlock x:Name="Separator2" Text="{Binding Separator}" Foreground="{Binding TextColorBrush}" Opacity="{Binding SeparatorOpacity}" Margin="4,-2,4,0" VerticalAlignment="Center"/>

				<TextBlock x:Name="SecondsText" Text="{Binding Seconds}" Foreground="{Binding TextColorBrush}" FontWeight="Medium" VerticalAlignment="Center"/>

			</StackPanel>
		</Viewbox>
    </Grid>
</widgets:BaseWidgetWindow>